using System;
using funda.common.auditing;

namespace funda.common
{
    public static class Utilities
    {
        public static void AddCreateAudit(IAuditable auditable)
        {
            //auditable.AuditEntries.Add(new )
        }
    }
}